# note-vault

Private note vault for [doing2done](https://github.com/d-cryptic/doing2done).
Parsed notes → VitePress → your host of choice (Cloudflare Pages recommended),
gated behind Cloudflare Access.

```bash
npm install
npm run docs:build   # outputs docs/.vitepress/dist
```
